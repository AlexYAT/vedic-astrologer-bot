## [Unreleased] (dev)

### Added
- Команда /admin: доступна только пользователям из MODE_SWITCH_USERS. MVP-статистика: пользователей (без исключённых), последняя активность (без исключённых). Исключённые = MODE_SWITCH_USERS (тестовые пользователи).
- Метрики /admin: активных за 24 часа, запросов за 24 часа, прошло с последней активности (Xч Ym).

### Improved
- /admin: выравнивание источника «Последняя активность» и «Прошло с последней активности» — тот же источник, что и requests_24h/active_24h (MAX(user_requests.created_at) с исключением MODE_SWITCH_USERS). Fallback на MAX(users.created_at), если user_requests пустая/отсутствует. Единый парсинг created_at как UTC, отображение в Europe/Paris. Отладочная подпись «Источник last_activity: user_requests|users» для админов.

---

## [0.5.0] - 2026-02-17

### Added
- Встроенный опрос onboarding_v1
- Сохранение ответов пользователей
- Бонус PRO на 3 дня за прохождение опроса
- Управляемый переключатель режима FREE/PRO (public/testers/off)
- Микро-метрика поведения после включения PRO
- Поддержка «время рождения неизвестно»: ввод «не знаю», «неизвестно», «нет», «примерно», «-», «?» или пустая строка; колонка `birth_time_unknown` в БД; прогноз по общему натальному фону и транзитам; предупреждение пользователю о пониженной точности

### Improved
- Улучшена аналитика поведения пользователей
- Подготовка к тестированию монетизации
- Управление состояниями диалога: сброс при /start, /menu, «Опрос», «Режим» и при нажатии любой кнопки меню; кнопка «📝 Опрос (бонус PRO)» в общем Regex меню; fallback в conv_handler и survey_conv при нажатии кнопок меню — устранён баг, когда после «Проверить действие» нажатие «Опрос» обрабатывалось как ввод действия
- Лог `conversation_reset user_id=... reason=...` при сбросе состояния

---

# Changelog

## [0.3.0] — 2026-02-15

### Added

- Новая схема БД: две таблицы — **users** (id, telegram_id, birth_*, thread_id, created_at, is_pro) и **user_requests** (логи запросов по типам: today, tomorrow, check_action, favorable, topic, setdata).
- Логирование каждого запроса к ассистенту: тип, опциональный текст, успех/ошибка (success), время ответа (response_time_ms) для аналитики и улучшения продукта.
- Мягкая миграция: при первом запуске старая таблица `users` переименовывается в `users_legacy`; данные переносятся в новую таблицу при первом обращении пользователя (lazy migration). Старые таблицы не удаляются.

### Changed

- Инициализация БД создаёт новые таблицы и индекс `idx_user_requests_user_id_created_at`.
- API слоя db: `get_or_create_user(telegram_id)`, `update_user_birth_data`, `get_user_birth_data`, `log_user_request`; прежние вызовы (`get_user`, `user_has_full_data`, `create_user`, `save_user_data`, `update_user`) сохранены для совместимости.

---

## [0.2.0]

- Централизованное хранение версии в `version.py`, логирование версии при старте.
